## 무엇을 했는가

<!-- 이 PR에서 한 일을 한두 줄로 -->

## 어떻게 테스트했는가

<!-- 예: backend `uvicorn app.main:app --reload` 후 /health 확인 / frontend `npm run dev` 화면 확인 -->

## 스크린샷

<!-- 화면 변화가 있으면 캡처를 붙이세요. 없으면 "해당 없음" -->

## 체크리스트

- [ ] main에서 분기했다 (`feat/<이름>` 또는 `fix/<이름>`)
- [ ] `.env`·비밀키를 올리지 않았다
- [ ] 로컬에서 lint·빌드가 통과한다
